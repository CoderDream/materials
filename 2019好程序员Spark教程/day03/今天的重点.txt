作业：
面向对象
	类的创建和属性的get、set方法的实现
	主构造器和辅助构造器的实现
	单例对象的理解
	伴生对象的理解
	apply方法的实现
	特质、抽象类、继承、实现
模式匹配
	各种模式匹配的理解

代码作业：
1.实现一个方法，使得返回数组中大于n的元素个数，等于n的元素个数，小于n的元素个数，并且一起返回。
	例：getNum(Array(1,2,3,2,4,1,5), 2) 结果为：(2,2,3)
	
2.实现一个方法，使得传进去的数组的元素两两交换。
	例：Array(1,2,3,4,5) 结果为：Array(2,1,4,3,5)