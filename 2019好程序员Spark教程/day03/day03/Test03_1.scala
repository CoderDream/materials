package com.qf.gp1922.day03

object Test03_1 {
  def main(args: Array[String]): Unit = {
    // 创建一个元组，获取元组中的值，遍历元组
//    val tup = (1,2,3,4,5,6,7)
//    for (i <- tup.productIterator) {
//      println(i)
//    }

    // 集合里有多个数组，每个数组的长度相同，将每个数组的相同下标下的元素进行聚合。
    // 例如：Seq(Array(2,3,4), Array(1,2,3), Array(4,5,6))
    // 结果为：Array(7,10,13)
//    val seq = Seq(Array(2,3,4), Array(1,2,3), Array(4,5,6))
//    // Array(3,5,7) zip Array(4,5,6)
//    val res = seq.reduce((x, y) => (x.zip(y)).map(x => x._1 + x._2)) // (x zip y) = Array((2,1), (3,2), (4,3))
//
//    println(res.toBuffer)


    val arr = Array(1,2,3,4)



  }
}
